"""Layer 1 — wire truth (ARCHITECTURE.md §3).

Faithful mirror of raw RentCast response shapes. Every field except `id` is
Optional; no behavior, no computed properties. Populated by F4-S2 against
`fixtures/live-samples/` ground truth.
"""
