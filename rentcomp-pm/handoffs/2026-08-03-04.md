---
status: UNCONSUMED
written: 2026-08-03
session: PM session 15
main_at_writing: edd93b2
---

# Handoff — session 15

`../QUEUE.md` is authoritative. **This handoff's predecessor (`2026-08-03-03.md`)
was written mid-flight before row 25a finished — 25a is now DONE too**, merged
at `0fbcb68`, deployed at `edd93b2`. Don't re-derive; `QUEUE.md` rows 16, 26,
44a, 25a all read DONE and are correct.

## Where things stand — four stories closed across sessions 13-14

**F5-S1, 44a, F10-S1, 25a** — all DONE, merged, pushed, `frontend/dist` rebuilt
and server restarted after each. This closes the re-scoped release's slot 2
(accurate bucket DOM) entirely. Owner said "commit push and keep going" —
everything was already committed/pushed at session end; only the untracked
`query_planner.py` mystery file sits on the tree, unchanged, still uninvestigated.

## Queued, ruled, not yet dispatched — pick up here

- **Row 47** — pre-existing `ws1-results-view.spec.ts` test false-fails on a
  genuine $1.00/sqft real comp (regex written for F0-S2's stub placeholder).
  Found independently by TWO QA agents in session 13. Ruled: fix the assertion
  to key on the actual stub sentinel, never a coincidental dollar value. Cheap,
  one assertion, test-only.
- **Row 46** — venv runs Python 3.14.0, `CLAUDE.md`/Stack section say 3.12.
  Disclosed independently by two devs. Ruled: decide the intended version
  FIRST, then make doc and venv agree — do not silently edit the doc to 3.14.

## Traps from sessions 13-14 that still apply, condensed (full detail in
`2026-08-03-01.md` and `-03.md`, both CONSUMED)

1. **"Merge into main" needs `git checkout main` FIRST, every time** — caught
   once this session-13 when a merge landed on `release/real-pull` instead.
   Confirm `git branch --show-current` reads `main` before every merge.
2. **Never run a browser-leg dispatch while the owner's app server holds port
   8000** — a real incident happened (spec wrote into the owner's live
   `~/.rentcomp/workspaces/`), fixed by row 44a, but the habit of stopping the
   server before dispatch and restarting + rebuilding `dist` after stays
   standing regardless.
3. **Two independent agents hitting the same defect is this project's signal
   for "give it a real row"** — not a third passing mention (this is how row
   47 came to exist).

## Still open with the owner, carried forward again

- Badge density (18.8% of comps flag `sqft_suspect` at 30%) — owner hasn't
  seen it on screen yet, tunable in one constant if it looks too noisy.
- Thin-cohort basis disclosure on `sqft_suspect` — real gap, not urgent, not
  yet its own row.
- Two `~/.rentcomp/workspaces/` test-artifact files from the incident — still
  there, owner's call to delete or leave.
- ⚠ Untracked `backend/src/rentcomp/pipeline/query_planner.py` — FOUR sessions
  running now with nobody having opened it or asked the owner directly. Worth
  actually resolving this session rather than carrying it a fifth time.

## What I would do next, in order

1. Row 47, row 46 — both cheap, both ruled, clear this backlog before starting
   a bigger story.
2. Actually look inside `query_planner.py` this time, or ask the owner
   directly what it is, rather than re-flagging it in another handoff.
3. The owner's hands-on pass — still the highest-value thing on their plate.
