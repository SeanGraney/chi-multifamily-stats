---
status: UNCONSUMED
written: 2026-07-29
session: PM session 8 (night squad — boots at F1-S2 dev dead holding uncommitted work)
main_at_writing: 6f28cf0
---

# Handoff — session 8

`../QUEUE.md` is authoritative for story state, rulings and counts — this file
carries only what dies with the session. Session 7's handoff
(`2026-07-28-03.md`, CONSUMED) is the prior thread; its traps still hold except
where superseded below.

## Mid-flight

Boot state: F1-S2's developer died with the session, holding **uncommitted**
work in worktree `agent-aea6736e9220bf994` — `api/workspaces.py` (174 lines)
and `storage/workspace.py` (344 lines) untracked, plus `app.py` (router wiring)
and `storage/pulls.py` (`pull_exists`) modified. Inspected read-only: coherent,
documented, not scratch. **Preserved as a labeled WIP commit before any
re-dispatch** — see the log entry.

**Live now:** `dev-F1-S2` (worktree `f1s2-dev`), `dev-F4-S4` (worktree `f4s4-dev`).
**Returned and accepted:** `qa-F4-S4`, `qa-F4-S8`, `qa-F3-S4`.
**All three QA phases are done — the queue is now dev-bound, not QA-bound.**

**F3-S4's dev dispatch must carry these, they are not in the story text:**
1. **It owns deleting F1-S2's strict xfail** (`test_a_reopened_workspace_derives_
   the_same_numbers_before_and_after_a_restart`). Deterministic because F1-S2
   merges first by my serialization ruling. An unowned XPASS-fail reads as a
   broken gate and gets "fixed" by re-adding the marker — re-hiding a bug that
   produces wrong numbers.
2. **The naive `as_of` fix is WRONG and QA proved it** — "advance if the held-
   signature set grew" freezes `as_of` forever, because a forced refresh
   overwrites the same files. It needs a *write happened* signal (the sink
   firing), not a set comparison.
3. **The manifest-in-memo test is owed now** (row 13a already scoped it).
4. Page-level resume is **out** (row 13c); leave the non-strict xfail alone.

**Dev serialization is the thing to hold in your head, because nothing in the
files enforces it.** F3-S4 shares `storage/pulls.py` with F1-S2 → **its dev
waits for F1-S2 to merge.** F4-S8 shares `pipeline/` with F4-S4 → **its dev
waits for F4-S4.** F4-S8's QA is done and accepted, so F4-S8's dev is the next
pipeline dispatch the moment F4-S4 lands.

**⚠ Branch trap — `story/F4-S4-qa` is CONTAMINATED and must never be merged.**
It carries 3 F4-S8 commits including a deliberately-red test. The clean branch
is **`story/F4-S4`** (QA's 4 commits cherry-picked; the dev is on it).
`story/F4-S8-qa` is clean.

## Traps and patterns

Session 7's list still holds in full. Additions this session are recorded below
as they are found.

- **A dead agent's worktree is checked for uncommitted work BEFORE re-dispatch,
  every time.** This is now two-for-two on the work being real and worth
  keeping. A reflexive re-dispatch would have thrown away 518 lines here.
- **EVERY parallel dispatch gets its own worktree — no exceptions.** INCIDENT #6:
  two QA agents shared one and their commits interleaved. INCIDENT #1's
  serialization ruling was written about *devs*; tonight proved **a QA agent
  creating a branch is a tree-touching act too.** Cheap to prevent, annoying to
  unpick.
- **WS-1 built more than its own scope.** Both F4-S4 and F4-S8 arrived with most
  of their implementation already on main (`pipeline/shape.py` steps 5–6,
  `buckets.py`). **This is not "the story is done"** — in both cases nothing had
  pinned the boundaries, and mutation testing found the holes were live. Expect
  the same on the remaining F4 stories, and expect the *real* work to be the one
  clause nobody built. **Ask QA to mutation-test when tests pass on arrival**;
  both agents did this unprompted and it is what made the returns trustworthy.
- **Neither F4 QA could be led into inventing a rule.** F4-S8 refused to invent a
  DOM clamp; F4-S4 refused to lock either answer on the unplanned-cohort
  question; F4-S8 left a mutation survivor alive rather than pin a `[DEFAULT]`
  over an `[INVARIANT]`. **Keep asking for judgment, not just green.**

## Owner items — status changed tonight, read before re-asking anything

- **Row 14a RULED: gloss only, no rename.** Now READY as a docs change.
- **Row 31 (F11-S4) DEFERRED by the owner. Do not re-ask unprompted** — same
  standing as the WS-1 sweep. Re-surface only when F11-S3 or F10-S2 nears DONE.
- **SKILLS_MAP RULED: rewrite to name real skills.** Done.
- **WS-1 exploratory sweep still pending since 7/27 — still do not re-ask.**

## What I would do next, in order

1. **Land F1-S2** — verify the 90 skips are zero, then merge and re-run the
   full gate on the **main checkout**.
2. **Then F3-S4's dev** — unblocked the moment F1-S2 merges. It now carries
   **five reproduced defects**, not just the `as_of` restamp; two of them
   (re-buying a parse-rejected 200, and 8-of-15 crash shapes re-buying) spend
   real API calls out of the 50-call budget.
3. **Then F4-S8's dev**, the moment F4-S4 lands — its QA is done and accepted,
   so it is a straight dispatch.
4. Row 14a is now a cheap docs-only win (NORTH_STAR gloss); good filler for a
   moment when the lanes are blocked on each other.
5. Rows 9a, 9b, 13b, 24a, 35a, 36 remain READY and small-to-medium.

## A pattern worth not losing

**A follow-up assigned to a role rather than to a queue row does not happen.**
ADR-001 named the PM for the `add-pipeline-stage` amendment on 2026-07-26. Four
PM sessions passed. No row carried it, so nobody did it, and the skill spent
three days telling readers to wire `premium` into the memoized group — which
ADR-001 had already ruled impossible. It was caught only because the owner's
SKILLS_MAP ruling made me open the file. **If a finding is real, it gets a row.**
