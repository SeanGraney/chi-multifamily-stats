---
status: CONSUMED
consumed: 2026-08-03
written: 2026-08-03
session: PM session 13
main_at_writing: d57755b
---

# Handoff — session 13

`../QUEUE.md` is authoritative. Session 12's handoff (`2026-08-03-01.md`,
CONSUMED) carries the stale-main repair and the F5-S1 AC/ruling trail — read it,
don't wait for me to restate it.

## ✅ F5-S1 DONE — merged, built, deployed. This section is now closed.

QA PASS (all 15 ACs), merged to `main` @ `08d3342`, `frontend/dist` rebuilt on
the main checkout (`npm install && npm run build`, session 11's lesson applied
— a merged frontend story is invisible until this happens), server restarted
serving `index-EyQtplUF.js`, fixture mode reconfirmed via `/proc/<pid>/environ`
(0 matches). Both story worktrees + branches removed per `WORKFLOW.md` §4 step 3.

⭐⭐ **A REAL MISTAKE, CAUGHT BEFORE IT SHIPPED WRONG — the merge protocol's
"merge into main" is not safe to run without checking out `main` first.** I ran
`git merge story/F5-S1` from the primary checkout while it was still on
`release/real-pull` (session start state) — git merges into **whatever branch is
checked out**, not into a branch named in the command. It landed on
`release/real-pull`; `main` stayed at `d57755b`, silently unchanged. Caught by
checking `git log --oneline -1 main` immediately after, out of habit, not because
anything looked wrong. **Fixed cleanly**: `main` was a strict ancestor of
`release/real-pull`'s new tip (0 commits the other way), so `git checkout main &&
git merge --ff-only release/real-pull` was lossless — a fundamentally different
situation from the earlier stale-main repair, which needed a real 3-way merge
because the two had diverged. **Rule for future sessions: `git checkout main`
is step zero of "merge into main," never assumed from context.**

## ⚠ INCIDENT LOGGED THIS SESSION (full detail in `QUEUE.md`, above row 16)

A Playwright spec (`f1-s2-recents.spec.ts`, F1-S2's, not F5-S1's) wrote two
test-artifact files into the **owner's real** `~/.rentcomp/workspaces/` by
colliding on port 8000 with the owner's live app server, which the PM had left
running. Verified read-only: the directory didn't exist before today, nothing
real was overwritten, files left in place untouched pending owner say-so. Root
cause and both fixes ruled and folded into **row 44a** (already existed for the
sibling defect class). **Standing rule now in force: never run a browser-leg
dispatch while the owner's app server holds port 8000.**

## Open with the owner — carried forward, not yet answered

- **Badge density: 91/485 = 18.8% flag at the 30% threshold**, about one row in
  five. Tunable via one constant (`SQFT_SUSPECT_THRESHOLD` in `premium.py`),
  QA's tests bracket it 10%/60% so tuning needs no test changes. Owner should
  see it on screen before any change.
- **Thin-cohort basis disclosure** (raised by `backend-reviewer`, ruled NOT a
  Semantic Change Protocol matter — the flag inherits `premium`'s documented
  basis exactly). Real disclosure gap, worth a follow-on story, not urgent.
- The two `~/.rentcomp/workspaces/` test-artifact files from the incident above
  — owner's call whether to delete or leave.
- ⚠ Untracked `backend/src/rentcomp/pipeline/query_planner.py` still on the
  primary tree, provenance unknown, still un-investigated.

## What I would do next, in order

1. Report F5-S1 DONE to the owner along with the incident and the open items above.
2. The owner's hands-on pass — now with a real, structured comp row to look at.
3. **25a**, then **F10-S1**'s verification pass (cheap — the code exists).
4. **Row 44a** — now widened to 2 fixes (`local-server.ts` env var + `f1-s2-recents`
   migration) plus the pre-existing serial-mode cleanup. Higher priority than
   before given today's incident.
