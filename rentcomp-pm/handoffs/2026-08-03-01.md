---
status: CONSUMED
consumed: 2026-08-03
written: 2026-08-03
session: PM session 12
main_at_writing: 3d0398a
release_branch: release/real-pull @ 3d0398a
---

# Handoff — session 12

`../QUEUE.md` is authoritative for story state, rulings and findings. Session 11's
handoff (`2026-07-30-01.md`, CONSUMED) is the prior thread and its five "must not
rediscover" items, deferral list, and traps still stand — read it, don't wait for
me to restate them.

## Mid-flight

**F5-S1 is IN_REVIEW. QA verify running** (agent `aac9ecc1d0cadce2e`, resumed into
the agent that wrote the tests). **I am holding the merge until it returns** —
charter rule, a verify that sees main move underneath reads like a merge that
dropped work.

- Worktrees: `.claude/worktrees/f5s1-qa` (`story/F5-S1-qa`), `.claude/worktrees/f5s1-dev`
  (`story/F5-S1`, dev agent `a4ee90823b594b29b`). Both committed, trees clean.
- Both reviewers CLEAN (`af4d32df064245ff3` backend, `a335f671b41eaf66c` frontend).
- **The one thing QA must not rubber-stamp: the dev's full Playwright run reported
  `112 collected: 86 passed, 25 skipped, 1 failed`** and attributed it to
  `local-server.ts`'s `VENV_RENTCOMP` not honouring `RENTCOMP_VENV_DIR`.
  **25 skipped is this project's most-recurring false-green shape**; I refused it
  on the dev's word. If the harness genuinely needs the one-line fix, **that is a
  shared-harness change and it is MINE to rule, not QA's to make.**

## ⭐ TRAPS THIS SESSION CLEARED — do not let them come back

1. ⭐⭐ **LOCAL `main` WAS STRANDED 395 COMMITS BEHIND `origin/main`** at the
   F4-S1 era (`59aa6a0`), while `sync.md` still documents the opposite invariant
   ("main runs 100+ commits ahead of origin/main by design"). **The release push
   inverted it and nobody noticed.** Dispatching QA would have had `/sync` merge a
   main from before the entire release. **Repaired by MERGE, not force-move**
   (`d57755b`), in a scratch worktree so the running app's checkout was never
   swapped underneath it; the 2 stale docs-only commits are preserved in history.
   `main` is now 7 ahead / 0 behind origin and the documented invariant holds again.
   ⚠ **`git branch -f` was BLOCKED by the permission classifier — correctly. The
   merge path needs no such permission and discards nothing. Prefer it.**
2. **Writing the AC list into the queue row before dispatch paid for itself
   immediately** — it is what surfaced AC6 and AC9 as backend work that the `[FE]`
   tag actively hides. Session 11's F6-S1 lesson generalises: **the queue row, not
   the story text, is the AC source of truth.**
3. **QA improved my AC6.** I specified "compute the >30% deviation"; QA found it
   **already exists** — `premium = psf/median − 1`, so the flag is
   `abs(premium) > 0.30` on the same median, and must live per-request so it moves
   as the user curates. **Ask QA to sharpen an AC, not just tabulate it.**

## Open with the owner — not escalated, needs a call

- **Badge density: 91/485 = 18.8% of comps flag at 30%, about one row in five.**
  Threshold is `[DEFAULT]` and QA's tests bracket it at 10%/60% rather than pinning
  0.30, so it tunes without touching the suite. Owner should see it on screen first.
- **Thin-cohort basis disclosure** (raised by `backend-reviewer`). When a cohort
  thins below `min_cohort_size`, F4-S5's median flips from *selected* to *pulled*
  comps, so the verify-sqft badge silently means "off the pulled median". **My
  ruling: NOT Semantic Change Protocol** — nothing's meaning changes, the flag
  inherits `premium`'s documented basis exactly and `premium_basis` is already on
  the row. **But it is a real disclosure gap → queue as a follow-on story.**
- ⚠ **Untracked `backend/src/rentcomp/pipeline/query_planner.py`** still on the
  primary tree. Provenance unknown — not from any story I dispatched. Judge it as
  a stranger's before trusting or deleting it.

## What I would do next, in order

1. **QA verify returns → rule on the 25-skipped question → merge both branches,
   mark DONE, push main.**
2. The owner's hands-on pass — still the highest-value thing on their plate, and
   now with a real comp row to look at.
3. **25a**, then **F10-S1**'s verification pass (cheap — the code exists).
4. **Row 44a** — two remaining `serial` markers, and now also `local-server.ts`'s
   `RENTCOMP_VENV_DIR` gap if QA confirms it.
