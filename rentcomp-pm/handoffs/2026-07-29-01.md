---
status: UNCONSUMED
written: 2026-07-29
session: PM session 8 (night squad — boots at F1-S2 dev dead holding uncommitted work)
main_at_writing: 6f28cf0
---

# Handoff — session 8

`../QUEUE.md` is authoritative for story state, rulings and counts — this file
carries only what dies with the session. Session 7's handoff
(`2026-07-28-03.md`, CONSUMED) is the prior thread; its traps still hold except
where superseded below.

## Mid-flight

Boot state: F1-S2's developer died with the session, holding **uncommitted**
work in worktree `agent-aea6736e9220bf994` — `api/workspaces.py` (174 lines)
and `storage/workspace.py` (344 lines) untracked, plus `app.py` (router wiring)
and `storage/pulls.py` (`pull_exists`) modified. Inspected read-only: coherent,
documented, not scratch. **Preserved as a labeled WIP commit before any
re-dispatch** — see the log entry.

**Four agents running as of this write** — `dev-F1-S2` (resuming in worktree
`agent-aea6736e9220bf994`), and QA-first on `qa-F3-S4`, `qa-F4-S4`, `qa-F4-S8`.

**Dev serialization is the thing to hold in your head, because nothing in the
files enforces it.** The three QA phases collide with nobody, but their devs do:
F3-S4 shares `storage/pulls.py` with F1-S2, and F4-S4/F4-S8 share `pipeline/`
with each other. **Do not dispatch F3-S4's dev until F1-S2 merges, and never run
F4-S4's and F4-S8's devs at the same time.**

Both F4 QA agents were sent a mid-flight correction about the stale skill
reference (see the log); `qa-F4-S4`'s scope was **narrowed** by it —
withdrawal-suspect is F4-S8's, not theirs.

## Traps and patterns

Session 7's list still holds in full. Additions this session are recorded below
as they are found.

- **A dead agent's worktree is checked for uncommitted work BEFORE re-dispatch,
  every time.** This is now two-for-two on the work being real and worth
  keeping. A reflexive re-dispatch would have thrown away 518 lines here.

## Owner items — status changed tonight, read before re-asking anything

- **Row 14a RULED: gloss only, no rename.** Now READY as a docs change.
- **Row 31 (F11-S4) DEFERRED by the owner. Do not re-ask unprompted** — same
  standing as the WS-1 sweep. Re-surface only when F11-S3 or F10-S2 nears DONE.
- **SKILLS_MAP RULED: rewrite to name real skills.** Done.
- **WS-1 exploratory sweep still pending since 7/27 — still do not re-ask.**

## What I would do next, in order

1. **Land F1-S2** — verify the 90 skips are zero, then merge and re-run the
   full gate on the **main checkout**.
2. **Then F3-S4's dev** (its QA should be back first), since F1-S2 must clear
   `storage/pulls.py` first. It carries the `as_of` restamp fix, which is
   producing wrong numbers today.
3. **F4-S4's dev, then F4-S8's dev — one at a time.**
4. Row 14a is now a cheap docs-only win (NORTH_STAR gloss); good filler for a
   moment when the lanes are blocked on each other.
5. Rows 9a, 9b, 13b, 24a, 35a, 36 remain READY and small-to-medium.

## A pattern worth not losing

**A follow-up assigned to a role rather than to a queue row does not happen.**
ADR-001 named the PM for the `add-pipeline-stage` amendment on 2026-07-26. Four
PM sessions passed. No row carried it, so nobody did it, and the skill spent
three days telling readers to wire `premium` into the memoized group — which
ADR-001 had already ruled impossible. It was caught only because the owner's
SKILLS_MAP ruling made me open the file. **If a finding is real, it gets a row.**
