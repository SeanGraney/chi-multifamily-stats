---
status: UNCONSUMED
written: 2026-07-29
session: PM session 9 (deadline day — owner ruled EXTEND; F4-S5 merged; F7-S1 one line from done)
main_at_writing: a46947d
---

# Handoff — session 9

`../QUEUE.md` is authoritative for story state, rulings and counts. Session 8's
handoff (`2026-07-29-01.md`, CONSUMED) is the prior thread; its traps all still
hold except where superseded below.

## ⚠ FIRST: THE API IS UNSTABLE. Four consecutive subagent deaths.

Two `500`s then two `529 Overloaded`, across **two different agents**, inside
about twenty minutes. **This is server-side and nothing to do with the work.**
Every death cost **zero** work because every agent had committed — keep
"commit early and often, and do not batch commits" in every dispatch, and on a
finishing task tell the agent to commit after *each* step.

If you boot into more of this: the F7-S1 tree survived four deaths unchanged.
Retry, don't reconstruct.

## Mid-flight — F7-S1 IS ONE LINE FROM DONE

**The only outstanding change in the entire session is:**

```
frontend/src/views/Results.tsx:347   useState(true) -> useState(false)
```

Then `git merge main`, then the three legs. Worktree `f7s1-dev`, branch
`story/F7-S1` @ `62ee235`, tree clean, main not yet merged.

**QA has already MEASURED that exact line taking the F7-S1 spec to `9 passed`
and the full suite to `61 passed, 0 failed`** — so a successor has a known-good
expected outcome and does not need to re-derive anything. Row 20 carries the
detail. QA wants a short confirm pass afterwards (a merge voids prior results).

⚠ **`story/F7-S1` and `story/F7-S1-qa` both point at `62ee235`. That is expected
and benign** — WORKFLOW §4 step 1 reached early, because the dying dev merged
QA's spec updates before it went down. **Not contamination.**

**Also in flight:** QA writing tests for **F4-S6** (`story/F4-S6-qa` @ `b002196`,
worktree `f4s6-qa`) and **row 10b** (`story/10b-qa` @ `1f06f66`, worktree
`r10b-qa`). Both have committed real work. Neither had reported when this was
written.

## Owner ruled the deadline: EXTEND, BUILD TO FULL MVP

Escalated per the charter on 7/29 and answered the same turn. **Do not
re-escalate; it is settled.** No new date was named — ask for one if it starts
to matter. The distinction worth not re-deriving: **the deadline's own literal
criterion is MET** (the tool prices a real unit end-to-end); **the MVP exit
gate's three conditions are NOT** (final regression, pre-MVP sweep, owner
fitness pass). Collapsing those two bars is the shortcut the gate exists to
prevent.

## What I would do next, in order

1. **Finish F7-S1** (one line, above), QA confirm, merge. Then **row 23 closes
   with it** — F7-S2 was folded into that dispatch.
2. **F8-S1 anchor computation** — unblocked by F4-S5 and now the critical path.
   ⚠ **Its dispatch MUST name row 11a's tension explicitly so its dev does not
   quietly pick an answer**: F8-S1 compounds `(1+d)^(currentYear − cohortYear)`
   with no bound on cohort age *and* no bound on cohort evidence. Row 11a has
   the measured numbers. Deciding whether such a number should be produced at
   all is likely the owner's, not the dev's.
3. **Row 10b before F5-S1 is dispatched** — F5-S1 unblocked the moment F4-S5
   merged, and 10b is a contract trap aimed at it.
4. **Row 44a** — raised in priority. It has now produced a wrong gate answer
   twice, and the second mechanism (async `serverProcess.kill()` inside our own
   suite) was previously unknown.
5. Then 35a, 36, F1-S1 (row 38 — **decide the anchor column AT DISPATCH**), 12b,
   44b, 45, and the READY backlog.

## Repo hygiene the classifier blocked — needs the owner's hand

**Fifteen stale branches could not be deleted** (permission classifier, same
block session 8 hit). **All fifteen are content-verified safe:**
`git diff --diff-filter=A main <branch>` is **empty for every one**, including
the three git calls unmerged. So `story/F4-S4-qa` can be deleted without losing
anything, which also removes session 8's never-merge trap.

```
git branch -D story/F4-S4-qa docs/14a-writeup worktree-agent-acc1706a40c06437d
```

Session 8's ten worktrees were already gone at boot; `git worktree prune`
cleared the records and `.venv` + `e2e/node_modules` were verified intact after.

## Process rulings made this session — all now in QUEUE.md or the charter

- **Boot ritual gains two checks, and both found real work on their first run.**
  (1) **Recompute `blockedBy` against the DONE set for every BLOCKED row before
  trusting the READY set** — found three stale cells, one of them the deepest
  chain in the project. (2) **Diff every story ID in the stories doc against the
  queue** — found `F4-S6` owned no row while nine other rows reasoned about it.
- **Resume by name while judgment is still needed; spawn fresh once the
  remaining work is transcription.** A 233K-token transcript became a liability
  once only three mechanical steps remained.
- **Fold a fix into a story when the story has already made the judgment and
  what remains is transcription. Refuse when the edit would itself decide
  something.** QA's distinction, and it finally makes row 13e's refusal and
  F4-S5's acceptance consistent rather than case-by-case.
- **D5's line falls between counting and combining:** counting a list you were
  given is not deriving a statistic; combining numbers you were given is. This
  will recur at F9-S1 and F10-S2.
- **Pin every ruling with a test.** F4-S5's proof: before the pin, the *rejected*
  reading passed the entire suite.
- **Relay predicates, not counts.** I said "the fourth instance" of a stale
  reference; the useful instruction was the predicate. A count invites stopping.
- **Port 8000 is solved** — ephemeral-port uvicorn + a live-child/banner identity
  check, now in `WORKFLOW.md` §2. It retires the stagger instruction for any spec
  that adopts it. Three specs already have.

## Things I got wrong this session, all caught by agents running the thing

Recorded because the pattern matters more than the instances: **five of my own
details have now been corrected by agents on this project, three of them today,
and every one by execution rather than argument.**

1. **Row 11a's door.** I said F4-S5's fallback was the cheap honest fix. It is a
   **no-op at n=1** — the premium stays exactly 0 by construction.
2. **The `comp_keys` "contradiction."** I framed it as two peer specs in
   conflict. One lives in `class TestProposedContract`, whose docstring already
   says the PM confirms or overrides. There was no conflict — there was an
   unconfirmed proposal awaiting my decision.
3. **A ruling issued mid-verify lands as a FAIL against work that could not have
   anticipated it.** F7-S1's only defect is my collapsed-footer ruling. The dev
   had flagged that exact tension and was right to.

## Owner items — five open, none blocking. Do not re-ask unprompted.

Unchanged from session 8: rows **11b**, **13d**, **14c**, **31 (F11-S4)**,
**38**, plus the **WS-1 exploratory sweep pending since 7/27**. Session 8's
framing of each still stands.

⚠ **Row 12b is new and may become an owner item** — a thin cohort's stated
evidence does not reproduce its displayed median, measured on the real pull.
Currently inert (nothing consumes the field), so it is queued rather than
escalated, but F8-S3 is the consumer that makes it visible.
