---
status: UNCONSUMED
written: 2026-07-30
updated: 2026-08-01
session: PM session 11 — the release shipped
main_at_writing: d398484
release_branch: release/real-pull @ d398484 (pushed)
---

# Handoff — session 11

`../QUEUE.md` is authoritative for story state, rulings and findings. This file
carries only what dies with the session. Session 10's handoff
(`2026-07-29-02.md` → `2026-07-29-03.md`, both CONSUMED) is the prior thread.

## ✅ THE RELEASE SHIPPED

**`main` @ `d398484`, pushed. Release branch `release/real-pull` cut from it and
pushed** (named for the capability that defines it — the analysis is now about
the pull you actually made; follows the existing `release/walking-skeleton`
convention rather than a version number).

**Six stories landed:** F7-S1 + F7-S2 (filter engine, strip, hidden footer) ·
**9b** (per-field identity selection) · **10b** (four repaired fixtures) ·
**F6-S1** (the Leaflet map) · **F4-S6** (pipeline progress + **the
search→Results seam**).

**Full gate, re-run by me on merged main:** pytest `3 failed / 2729 passed /
1 skipped / 4 xfailed` (the 3 = row 24a's documented Windows failures) · vitest
`9 passed` · Playwright **`95 collected = 95 reported, 0 failed, 0 skipped,
0 did-not-run`**. Ledger untouched all session: **8 spent / 42 remaining.**

**Verified by running it**, not inferred: `rentcomp` serves HTTP 200, the API
answers, the console is clean, and it serves `index-BQCkThfE.js` — the exact
bundle built from this merge. The landing page reads *"Start with a search"*,
which **is** the seam: there is no longer any door into Results except a real
search.

## ⚠ THE FIVE THINGS A SUCCESSOR MUST NOT REDISCOVER THE HARD WAY

1. **`frontend/dist` is GITIGNORED, so merging a frontend story does NOT make
   the owner see it** — `rentcomp` serves that directory. And `npm run build`
   will **fail** on main after a story adds a dependency, because main's
   `node_modules` predates it. ⭐ **`npm install && npm run build` on the MAIN
   CHECKOUT is part of DONE. A green suite with an invisible feature is the
   delivery version of a false green.**
2. ⭐ **Cross-story merges are the PM's, and this session proved why.** F6-S1 and
   F4-S6 were each green alone and **red together** — four specs navigated to
   Results and waited for a derive the seam no longer permits. **Green alone, red
   together is the failure class only a merge produces.** Neither story was at
   fault; the specs predated the seam.
3. ⭐ **`mode: "serial"` concealed ELEVEN PASSING TESTS behind one failure.**
   Removing it turned `1 failed / 11 did-not-run / 4 passed` into
   `1 failed / 15 passed / 0 did-not-run`. **Row 44a is not hygiene.** ⚠ **Two
   specs still carry it** (`f0-s1b-integration:49`, `f1-s2-recents:77`); the
   latter is quiet **only while its three xfails keep failing**, so it springs
   **the day F1-S1 lands** — exactly when someone is reading the result.
4. **I marked two tests strict-xfail myself** (`calls_to_complete`, on my own
   deferral behind row 13j). **A PM editing a test — flagged for review.** They
   flip to hard red the day that rendering lands, which is when 13j must be read.
5. **Machine throughput, not agent availability, was the real parallelism limit.**
   The suite grew 1,831 → 2,729 tests at ~95–105s a run, and every agent runs it
   several times. **Cap full-suite agents at 3–4 concurrent**; plan-writing QA can
   fan out wider. The charter's rule covers *file* contention and does not name
   this one.

## What the owner deferred — informed, with the cost stated

**F5-S1** (the `$/sqft` verify flag stays hardcoded `False`, so 9b's admitted
comp at **$4.28/sqft, +156.7%, a 3-bed/3-bath in 700 sqft** renders with **no
warning**, every session, under a frozen pull) · **25a** (the bucket panel can
show a median built from **one** observation) · **F5-S3** (cheapest to defer —
`Analysis.tsx` is confirmed a **13-line placeholder**; it gates a door to an
empty room) · **9d** · **F10-S1** (⚠ **not a build — a VERIFICATION PASS**;
`bucket_stats()` already computes every §150 statistic and `<BucketTable>`
already renders it. Cheapest row in the queue).

**The cache is cut from the critical path** by owner decision, on the measured
finding that a repeat pull already costs **zero calls** and that one end-to-end
prediction costs **2 per cohort year + 1 per extra 500 records** (~7 for the
owner's search; 42 remaining ≈ 5–6 predictions). **Mitigation agreed instead of
13c: verify completeness at pull time, and copy `~/.rentcomp/` after a good
pull — restore beats re-buy.**

## Live state on disk

**Kept deliberately:** `r13c-dev` (⚠ holds **162 preserved lines** of round-4's
MB1 fix, labelled NOT-run — it appears to implement both design constraints but
**nothing was ever executed**, and the `cache.py` disclosure correction is
**absent** from the diff) · `r13c-qa` · `f10s1-qa` (2 commits, partial plan).
Everything else removed after a clean reparse-point scan; **main's `.venv`,
`e2e/node_modules` and `frontend/dist` verified intact.** One empty directory
(`f4s6-qa`) survived a file lock — already untracked by git, sweep any time.

## What I would do next, in order

1. ⭐ **The owner's hands-on pass — the remaining gate, and the highest-value
   thing on their plate.** Green tests were never sufficient. Precedent: the
   hardcoded-`PULL_REF` seam survived **eleven merged stories** and was found by
   inspection, not by any suite. Findings become stories, not patches.
2. **F5-S1**, then **25a** — the two guardrails that stop the tool overstating
   what it knows. F5-S1 has a named real-data fixture waiting on row 16: an
   implementation that does not flag that comp has not implemented the flag.
3. **F10-S1's verification pass** (cheap — the code exists).
4. **Row 44a** — the two remaining `serial` markers and `ws1-results-view`'s
   hardcoded port 8000. **This session cost real time to that row twice.**
5. **13c round 4** if the cache lane reopens: resume from `eef1b8e` and **judge
   the preserved lines as a stranger's**.

## Traps still standing from session 10 (unchanged, still true)

`MAX_LIMIT` vs observed `page_size` must **not** be unified · N5 and the
MAX_LIMIT/`page_size` "contradiction" are **not** contradictions · `_LEASED_CLASSES`
excluding `pending` in `buckets.py:46` is settled and load-bearing · **F10-S1 does
NOT depend on the anchor** (verified in code; it enters only via `_dollars()`).

## Agent-handling notes worth carrying

- **Resume by `agentId`, not by name** — after a mid-run death the name often
  fails to resolve while the ID still works. Keep the ID of everything you spawn.
- ⚠ **"Resume by name" does NOT survive a session boundary.** Worktrees survive;
  agent contexts do not. Every such "resume" is really a fresh spawn and the
  dispatch must carry the preserved-tree context explicitly.
- **A wave of simultaneous 529s is capacity, not your dispatch.** Resume; do not
  rewrite.
- ⭐ **"Commit early and often" paid for itself outright.** The session crashed
  with both release devs mid-flight and **both trees were clean** — zero lines
  lost. Earlier agents died holding 361 and 162 uncommitted lines.
- ⭐ **When an agent goes silent, check whether its work is already committed
  before re-dispatching.** F4-S6's verify sat for three hours having *written*
  its probes but never run them. **Running them myself took 23 seconds** and
  produced the release's decisive fact (the seam is closed, 5/5).
- ⭐ **Point agents at a working example, not a description.** Three attempts at
  the map plan failed against prose; the fourth succeeded once F7-S1 was on main
  to copy — "most of a day saved", per that agent.
- ⭐ **The root cause of those three failures was that F6-S1 HAS NO ACCEPTANCE
  CRITERIA** — one line of prose, no `AC:` anywhere. Asking for "a table with
  every AC once" asked three agents to tabulate a list that does not exist.
  **Write the AC list into the queue row so QA tabulates rather than drafts.**
