---
status: UNCONSUMED
written: 2026-07-27
session: PM session 4 (boots at "both checkpoint-2 gates clear, parallel dispatch open")
main_at_writing: 179a4c5
---

# Handoff — 2026-07-27 (session 4)

`../QUEUE.md` is authoritative for story state and rulings. Session 3's handoff
(`2026-07-27-03.md`, CONSUMED) is the live thread — read it in full if this file
is thin; not restating it here.

## Mid-flight

Parallel dispatch opened, using `isolation: "worktree"` for real git
parallelism (sidesteps the earlier INCIDENT-#3/#4 shared-tree race class).
Both F3-S1 and F2-S2 are now IN_DEV, dev dispatched on both, each in its own
isolated worktree:

- **F2-S2** (bed/bath range parser): **DONE.** Merged to main (`--no-ff`,
  PM-executed, QA correctly did not merge itself). 652/652 pytest on main,
  PM-reverified independently. Branches + finished worktrees cleaned up.
- **F3-S1** (cache key + raw store): QA done, PM ruled 3 flagged items
  (normalize address casing/whitespace for real, approved invented cache
  contract, ledger seeding lazy-on-read not migration). **Dev still running**
  (agent ad4f55f261ffa191f, worktree `agent-ad4f55f261ffa191f`, locked — do
  not touch that worktree until it reports). Not yet reported.

**New WORKFLOW.md note added this session:** isolated worktrees share one
`.venv/` rooted at the main checkout — `pytest` inside a worktree needs
`PYTHONPATH=<worktree>/backend/src` prepended or it silently tests the wrong
tree's code. Found by the F2-S2 dev, folded into WORKFLOW.md §2.

**Worktree-cleanup pattern discovered this session, worth repeating:** once
an agent's branch is merged to main, `git worktree remove <path>` + `git
branch -d <branch>` cleans up — but `git worktree list` shows `locked` on
a still-running agent's worktree; skip those, only clean up finished ones.
A QA agent's own ad-hoc verification branch (created because the canonical
`-qa` branch was checked out in a sibling worktree it couldn't re-checkout)
needed `-D` not `-d` since it was a separate merge-commit chain with
byte-identical-but-not-ancestor content — force-delete is fine there, it
was a scratch branch, not lost work.

Repo: `main` @ `2d6e433`, working tree clean except the still-running F3-S1
worktree under `.claude/worktrees/`.

## What I would do next, in order

1. When F3-S1 dev reports: spot-check per IN_REVIEW gate, run
   `backend-reviewer` if dev's environment again lacks the Agent tool
   (seems to be a pattern for worktree-isolated dev agents — may be worth
   asking why in a future session rather than routing around it every
   time), dispatch QA verify, then execute the merge + cleanup on PASS
   (same pattern just used for F2-S2).
2. Once F3-S1 is DONE too: next candidates per session-3's plan are
   F4-S1..S8 formal per-story QA passes (WS-1/WS-1a were minimal, not
   story-grade) — F4-S3 especially, checkpoint-2 flagged a real `_stitch`
   negative-gap bug waiting there.
3. Still watching for the owner's exploratory-sweep response — don't re-ask.

**Lesson this session:** when editing QUEUE.md with an Edit tool old_string
match, double-check the exact story ID substring — a one-character typo
(`F2-S1` vs `F2-S2`) in the search string produces a silent "not found"
that looks like a file-content mystery, not a typo, and burns a few tool
calls chasing a phantom encoding issue before spotting it.
