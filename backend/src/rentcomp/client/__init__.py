"""RentCast API client (ARCHITECTURE.md §2): rentcast.py, fixture | live.

Populated by F0-S4. The app's network path is httpx + the .env key only —
never an MCP. Live mode requires RENTCOMP_LIVE=1 AND a key (D17).
"""
